const app = require('../server/server');

// This file serves as the main entry point for Vercel's Serverless Functions.
// It imports the Express app from our existing server logic.
module.exports = app;
